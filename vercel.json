{
  "version": 2,
  "outputDirectory": "dist/client",
  "buildCommand": "npm run build",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}